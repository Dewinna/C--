/*
 ============================================================================
 Name        : drinkmachine.h
 Author      : Anthony Swink
 Version     : 1
 Copyright   : ples no take
 Description : used to test the create and destroy functions
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include"drinkmachine.h"

/*
int main(void){
	DrinkMachine *drinkMachine;
	drinkMachine = create();
	printf("Id\tName\t\tPrice\tQty\tSold\n");
	for(int i=0; i < drinkMachine->numItems; ++i){
		printf("%d\t%-10s\t%.2lf\n", drinkMachine->drinkItem[i].id , drinkMachine->drinkItem[i].name, drinkMachine->drinkItem[i].price);
	}
	fflush(stdout);
	destroy(drinkMachine);

	return EXIT_SUCCESS;
}

*/
